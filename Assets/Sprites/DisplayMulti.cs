using UnityEngine;
using System.Collections;

public class DisplayMulti : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //設定雙螢幕的解析度
        Screen.SetResolution(2732, 768, true);
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
